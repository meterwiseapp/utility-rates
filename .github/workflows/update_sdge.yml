name: SDGE Auto/Manual Update
on:
  schedule:
    - cron: '0 12 1 * *' 
  workflow_dispatch: 
    inputs:
      dry_run:
        description: 'Dry Run'
        required: true
        type: boolean
        default: false
  # Trigger when a PDF is committed to the uploads folder
  push:
    paths:
      - 'upload_folders/sdge_uploads/**.pdf'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4 

      - name: Setup Python
        uses: actions/setup-python@v5 
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: pip install requests beautifulsoup4 pdfplumber pypdf

      - name: Run Automated Scraper
        # This keeps the dates fresh if no PDF is uploaded
        run: python scrapers/update_sdge_rates.py ${{ github.event.inputs.dry_run == 'true' && '--dry-run' || '' }}

      - name: Run Manual PDF Parser
        # This overwrites web data with the high-precision PDF data
        run: python parsers/parse_sdge_manual.py ${{ github.event.inputs.dry_run == 'true' && '--dry-run' || '' }}

      - name: Commit and Push
        if: github.event.inputs.dry_run != 'true'
        run: |
          git config --global user.name 'SDGE-Bot'
          git config --global user.email 'bot@utilityestimator.com'

          TODAY=$(date -u +"%Y-%m-%d")
          jq --arg d "$TODAY" '.lastUpdated = $d' rates/sdge_rates.json > tmp.json && mv tmp.json rates/sdge_rates.json
          
          git add rates/sdge_rates.json
          cp rates/sdge_rates.json sdge_rates.json || true
          git add sdge_rates.json
          git commit -m "SDG&E rate update: $(date)" || exit 0
          git push
